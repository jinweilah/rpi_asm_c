#include<stdio.h>

void controlstatus(status)
{
    if(status =0)
    {
        off code;
    }
    else if (status = 1)
    {
        on code;
    }
    else if(status = 2)
    {
        blinkking working code
        
    }
}